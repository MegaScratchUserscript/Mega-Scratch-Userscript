// initializer, creates the unsafeWindow.msuParts object
msuParts = {};
// access window.jQuery instead of whatever jquery Greasemonkey may be putting in
// $ = unsafeWindow.$;
// jQuery = unsafeWindow.jQuery;
